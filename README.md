# http-server

Basic HTTP server in C with testing using the Unity framework.

## Usage
Compile files
```bash
make
```
Run the server
```bash
./http-server
# Then open localhost:8080 in a browser.
```
Remove object files
```bash
make clean
```
Run tests
```bash
make test
```
